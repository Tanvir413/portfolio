 // Get the current year
 const currentYear = new Date().getFullYear();
        
 // Set the current year inside the element with id 'year'
 document.getElementById("year").textContent = currentYear;